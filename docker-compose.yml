services:
  app:
    build: .
    ports:
      - "80:3000"
    volumes:
      - uploads:/app/uploads
      - dbdata:/app/data
    env_file:
      - .env.production
    restart: always

volumes:
  uploads:
  dbdata:
